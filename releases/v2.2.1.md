# v2.2.1 开发日志

## 基本信息

- **版本号**：v2.2.1
- **发布日期**：2026-09-22
- **类型**：小版本（Bugfix + Audit）
- **Commit**：`0fa3f534`

## 核心目标

全局代码审计修复：查重、查错、查漏。

## 查重修复

- 新增 `gossip.rs` 桩文件：补全 GossipSub 模块结构
- 新增 `transport.rs` 桩文件：补全传输层结构
- 消除重复的网络初始化代码

## 查错修复

- Python `Task.status` 裸字符串改为 `TaskStatus` 枚举
- `sharded.py` counts 统一写入 ShardMetadata
- `ReputationBridge.syncReputation` 补 `MAX_REPUTATION` 校验
- Rust unused warnings 清理

## 查漏修复

- 新增 `get_shard_stats()` 查询接口
- 分片过载自动翻倍分裂（上限 16）
- 签名负例测试：验证错误签名被拒绝

## 测试

- 10 个 Rust 测试全部通过
- 7 个 Python 测试全部通过
- Foundry 17 个测试全部通过
- **Total: 34/34 passed**
