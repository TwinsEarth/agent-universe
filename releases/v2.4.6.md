# v2.4.6 - 群体智能飞轮（Flywheel）

## 一句话概述

v2.4.6 把跨模型协同、分层网络、三层记忆串成自加速飞轮：协同产生经验 → 经验反哺结构 → 优化后再协同。

## 闭环

1. `collaborate(k)`：跨模型协同，独立模型 k ≥ 3 才有集成红利（误差独立性决定上限）。
2. `ingest(n)`：三层记忆吞入经验。
3. `optimize_structure()`：经验反馈优化结构。
4. 回到 ①，飞轮自加速。

## 验证

- `is_spinning()` 校验闭环条件；单一模型不构成飞轮，测试全绿。
