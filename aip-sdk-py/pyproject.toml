[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aip-sdk"
version = "2.2.3"
authors = ["Agent Universe"]
description = "Agent Interop Protocol SDK"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
