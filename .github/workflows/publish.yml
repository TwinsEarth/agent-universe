name: Publish Package

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. v2.3.4)'
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  publish-rust:
    name: Publish Rust Crate to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Configure cargo registry
        run: |
          mkdir -p ~/.cargo
          printf '[registries.github]\nindex = "sparse+https://pkg.github.com/TwinsEarth/agent-universe/"\nprotocol = "sparse"\n' > ~/.cargo/config.toml
          echo "${{ secrets.GITHUB_TOKEN }}" | cargo login --registry github
          cat ~/.cargo/config.toml

      - name: Set crate version from tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME}"
          fi
          CRATE_VERSION="${VERSION#v}"
          echo "Publishing version: $CRATE_VERSION"
          cd gsn-core
          sed -i "s/^version = .*/version = \"$CRATE_VERSION\"/" Cargo.toml
          grep '^version' Cargo.toml

      - name: Build
        run: cd gsn-core && cargo build --release

      - name: Test
        run: cd gsn-core && cargo test

      - name: Publish to GitHub Packages
        run: cd gsn-core && cargo publish --registry github --allow-dirty
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-python:
    name: Publish Python SDK to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Set package version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME}"
          fi
          CRATE_VERSION="${VERSION#v}"
          cd aip-sdk-py
          sed -i "s/^version = .*/version = \"$CRATE_VERSION\"/" pyproject.toml
          grep '^version' pyproject.toml

      - name: Build package
        run: cd aip-sdk-py && python -m build

      - name: Publish to GitHub Packages
        run: |
          cd aip-sdk-py
          python -m twine upload \
            --repository-url https://upload.pkg.github.com/TwinsEarth/agent-universe \
            --username "__token__" \
            --password "${{ secrets.GITHUB_TOKEN }}" \
            dist/*
